#!/usr/bin/env bash
# ═══════════════════════════════════════════════════════════════════════════════
# .env.sample — copy to .env and adjust for your environment
# ═══════════════════════════════════════════════════════════════════════════════

# --- OpenAI ---
# Required for both openai-images-generate and openai-images-edit tools
# OPENAI_API_KEY=sk-...replace-me...

# --- Optional: Azure OpenAI ---
# If set, the server will use AzureOpenAI instead of the standard OpenAI client
# AZURE_OPENAI_API_KEY=sk-...replace-me...
# AZURE_OPENAI_ENDPOINT=https://your-instance.openai.azure.com
# OPENAI_API_VERSION=2024-12-01-preview

# --- Google GenAI (Gemini / Veo) ---
# Used by planned Google video tools (see plan/google_video_support.md).
# GEMINI_API_KEY=...replace-me...
# Alternatively (Gemini Developer API):
# GOOGLE_API_KEY=...replace-me...
#
# Vertex AI mode (requires ADC / service account credentials):
# GOOGLE_GENAI_USE_VERTEXAI=true
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=europe-west4

# --- media-gen-mcp local file handling ---
# Comma-separated list of directory patterns (absolute or relative; supports * and ** globs).
# If unset, defaults to /tmp/media-gen-mcp (or %TEMP%/media-gen-mcp on Windows).
# Examples:
#   MEDIA_GEN_DIRS=media/                     # relative to current working directory
#   MEDIA_GEN_DIRS=/home/*/media-gen-mcp/     # any user's media-gen-mcp
#   MEDIA_GEN_DIRS=/data/**/images/           # any depth under /data ending in images/
MEDIA_GEN_DIRS=media/,samples/

# Optional comma-separated list of allowed HTTP(S) URL patterns.
# Supports * (single segment) and ** (any depth) globs.
# Leave empty to allow any HTTP(S) URL.
# Examples:
#   MEDIA_GEN_URLS=https://*.cdn.example.com/
#   MEDIA_GEN_URLS=https://storage.example.com/**/assets/
MEDIA_GEN_URLS=

# Optional HTTPS prefixes for public URLs that mirror MEDIA_GEN_DIRS entries.
# This is a comma-separated list matched positionally to MEDIA_GEN_DIRS.
# Example: MEDIA_GEN_MCP_URL_PREFIXES=https://media-gen.example.com/media,https://media-gen.example.com/samples
#MEDIA_GEN_MCP_URL_PREFIXES=

# --- Logging ---
# Log level: debug, info (default), warn, error, silent
MEDIA_GEN_MCP_LOG_LEVEL=info
# Log format: text (default) or json (structured)
# MEDIA_GEN_MCP_LOG_FORMAT=json

# --- Test tool configuration ---
# Enables test-tool for debugging MCP result placement without calling OpenAI API.
# Set to enable the test-tool (reads sample images from this directory).
# Security: only exact path matches are allowed — no path traversal.
MEDIA_GEN_MCP_TEST_SAMPLE_DIR=media/samples/
